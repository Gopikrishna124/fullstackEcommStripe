import { createContext } from "react";      
                                        
 const Context=createContext(null)

 export default Context

 //we created contextApi only to share userdetails function in all components
 